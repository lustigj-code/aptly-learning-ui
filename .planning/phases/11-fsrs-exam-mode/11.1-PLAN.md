# Phase 11.1: FSRS Dashboard Integration

**Phase:** 11 - FSRS Wiring + Exam Mode
**Plan:** 11.1 - Dashboard Widget + Session Integration
**Priority:** High (review visibility critical for retention)
**Estimated Effort:** 2-3 hours

---

## Context

The FSRS system is 80% complete:
- **COMPLETE:** `/src/lib/mastery/fsrs.ts` - Full FSRS-4.5 algorithm
- **COMPLETE:** `/src/hooks/useReviewQueue.ts` - Hook for fetching due items
- **COMPLETE:** `/src/app/review/page.tsx` - Dedicated review page UI
- **COMPLETE:** `/api/review/due` and `/api/review/complete` - APIs working
- **COMPLETE:** Atom completion auto-schedules FSRS reviews

**What's Missing:**
1. Dashboard widget showing review count + quick access
2. Review items interleaved into learning sessions (not just separate page)

---

## Objective

Make review due items visible on dashboard and optionally interleavable into learning sessions.

---

## Tasks

### Task 1: Create ReviewQueueWidget Component

**File:** `src/components/dashboard/ReviewQueueWidget.tsx`

Build a dashboard card showing review status:
- Display due item count with "N items due" badge
- Show top 3 concepts needing review (names only)
- "Start Review Session" button linking to /review
- Visual indicator when 0 items due (green checkmark)
- Show next review time if nothing due now

```typescript
interface ReviewQueueWidgetProps {
  userId: string;
}
```

**Commit:** `feat(11-01): Add ReviewQueueWidget component`

---

### Task 2: Integrate Widget into Dashboard

**File:** `src/app/dashboard/page.tsx`

Add the review widget to the dashboard layout:
- Place in right sidebar or as prominent card
- Only show when user has active courses
- Use existing useReviewQueue hook
- Conditionally show based on feature flag (for A/B testing)

**Commit:** `feat(11-01): Integrate ReviewQueueWidget into dashboard`

---

### Task 3: Add Review Badge to Learning Header

**File:** `src/components/learning/CoachLearningView.tsx` (or similar)

Show subtle indicator when reviews are due during learning:
- Small badge/pill showing "N reviews due" in header
- Click to switch to review mode
- Non-intrusive but visible

**Commit:** `feat(11-01): Add review due badge to learning header`

---

### Task 4: Create Review Interleaving Option

**File:** `src/lib/adaptive/sessionBuilder.ts` (or similar)

Add option to interleave review items into sessions:
- Query FSRS due items at session start
- Insert 1-2 review items between every 3-4 new items
- Mark interleaved items with "Review Challenge" badge
- Respect user preference for interleaving vs separate review

Note: Full interleaving is Phase 13 scope - this is basic support only.

**Commit:** `feat(11-01): Add basic review interleaving to session builder`

---

## Testing

1. Complete an atom to trigger FSRS scheduling
2. Navigate to dashboard
3. Verify ReviewQueueWidget shows due count
4. Click "Start Review Session"
5. Verify review page loads with due items
6. During learning, verify header shows due badge (if items due)

---

## Success Criteria

- [ ] ReviewQueueWidget displays on dashboard
- [ ] Due count accurate and updates after reviews
- [ ] Can navigate to /review from widget
- [ ] Review due indicator visible in learning UI
- [ ] Build passes

---

## Notes

- Keep widget simple - detailed review analytics come later
- Interleaving here is basic; Phase 13 adds semantic relevance filtering
- The review page at /review already handles the actual review flow
