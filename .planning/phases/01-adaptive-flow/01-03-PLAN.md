---
phase: 01-adaptive-flow
plan: 03
type: execute
---

<objective>
Complete adaptive flow with interleaved reviews and session completion tracking.

Purpose: Make the adaptive experience seamless by naturally mixing reviews with learning and showing clear session progress.

Output: Reviews appear naturally during sessions, session progress visible, clean completion flow.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./01-03-SUMMARY.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-adaptive-flow/01-01-SUMMARY.md
@.planning/phases/01-adaptive-flow/01-02-SUMMARY.md

**Key source files:**
@src/app/learn/page.tsx (modified in plans 01 and 02)
@src/components/learning/ReviewTab.tsx (existing review component)
@src/lib/adaptive/sessionBuilder.ts (interleaveReviews function may exist)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session progress indicator</name>
  <files>src/app/learn/page.tsx</files>
  <action>
When in adaptive mode with activeSession, show session progress in header:

1. Replace or augment the existing progress bar:
   - Show "Session: X of Y items"
   - Progress bar based on sessionItemIndex / session.items.length
   - Show estimated time remaining

2. Add session summary chip/badge:
   - "2 reviews + 1 lesson + quiz"
   - Update as items complete

3. Style to match existing header:
   - Keep consistent with current ProgressBar component
   - Use teal color for session progress
  </action>
  <verify>npm run build succeeds, session progress visible in header when in adaptive mode</verify>
  <done>Session progress indicator shows current/total items and estimated time</done>
</task>

<task type="auto">
  <name>Task 2: Create inline review cards for session flow</name>
  <files>src/components/learning/SessionReviewCard.tsx</files>
  <action>
Create compact review card component for use within sessions:

```typescript
interface SessionReviewCardProps {
  skillId: string;
  type: 'warmup' | 'review' | 'cooldown';
  onComplete: (correct: boolean) => void;
}
```

Features:
- Show skill name and one review question
- Quick answer UI (2-4 options)
- Immediate feedback (correct/incorrect)
- Call onComplete with result
- Smooth transition animations

Different styling based on type:
- warmup: "Quick warmup" badge, amber tones
- review: "Reinforce learning" badge, teal tones
- cooldown: "Lock it in" badge, green tones

Fetch question from review/due API or generate based on skillId.
  </action>
  <verify>npm run build succeeds, SessionReviewCard.tsx exists, renders a review question</verify>
  <done>SessionReviewCard component created with skill-based review question</done>
</task>

<task type="auto">
  <name>Task 3: Wire session completion and clean up</name>
  <files>src/app/learn/page.tsx</files>
  <action>
1. Handle session completion:
   - When sessionItemIndex >= session.items.length:
     - Show session completion celebration (confetti)
     - Display session summary: "Session Complete! X skills reinforced, Y new concepts"
     - Button to "Start Another Session" or "Return to Dashboard"

2. Clean up mode toggle:
   - Remove the debug toggle if desired, or keep hidden behind query param (?mode=linear)
   - Default to adaptive mode

3. Preserve edge cases:
   - If session has 0 items, fall back to linear mode with message
   - If build session fails, fall back to linear mode gracefully
   - If user is mid-lesson, don't interrupt with new session (continue existing)

4. Remove unused imports and dead code from learn page related to old flow that's now in LinearLessonView.
  </action>
  <verify>npm run build succeeds, completing all session items shows completion, no console errors</verify>
  <done>Session completion flow works, edge cases handled, code cleaned up</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete adaptive learning flow with session builder, pre-tests, interleaved reviews</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000/learn
    3. Verify: Session overview appears with "Your Personalized Session"
    4. Click: "Start Session"
    5. Verify: First item appears with reasoning banner
    6. Complete: 2-3 items, verify progress updates
    7. Test: Pre-test by navigating to new lesson (if available)
    8. Verify: No console errors, smooth animations
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npm run lint` passes
- [ ] Session progress indicator visible during adaptive mode
- [ ] SessionReviewCard renders within sessions
- [ ] Session completion shows celebration and summary
- [ ] Fallbacks work (empty session, build failure)
- [ ] No dead code or unused imports
</verification>

<success_criteria>

- Phase 1 complete: Adaptive learning flow fully functional
- Session-based navigation replaces linear navigation
- Pre-tests allow content skipping
- Reviews interleaved naturally
- Clean, maintainable code
</success_criteria>

<output>
After completion, create `.planning/phases/01-adaptive-flow/01-03-SUMMARY.md`:

# Phase 1 Plan 3: Session Completion and Reviews

**[Substantive summary]**

## Accomplishments

- Session progress indicator
- Inline review cards
- Session completion flow
- Code cleanup

## Files Created/Modified

...

## Phase 1 Complete

Adaptive learning flow now replaces linear navigation. Users see personalized sessions, can test out of content, and reviews are interleaved naturally.

Ready for Phase 2: Complete Missing Components
</output>
