---
phase: 01-adaptive-flow
plan: 01
type: execute
---

<objective>
Replace linear learn page navigation with session-based adaptive flow using the existing AdaptiveSessionView component.

Purpose: Transform the learning experience from "next/prev atom" to personalized session-based learning where the system decides what to teach next.

Output: Learn page renders AdaptiveSessionView, builds sessions on load, and shows session overview before learning starts.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./01-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Key source files:**
@src/app/learn/page.tsx (649 lines - needs refactor to use adaptive flow)
@src/components/learning/AdaptiveSessionView.tsx (ready to use, just needs integration)
@src/lib/adaptive/sessionBuilder.ts (buildSession function exists)

**Established patterns:**
- Zustand for client state
- 'use client' directive for interactive components
- framer-motion for animations
- cn() utility for className merging

**Constraints:**
- Must preserve existing ReviewTab functionality
- Must preserve mastery gates
- Must preserve proactive coach integration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract linear navigation to separate component</name>
  <files>src/components/learning/LinearLessonView.tsx</files>
  <action>
Create new component that contains the current linear lesson flow from learn page:
- Move atoms array navigation (currentAtomIndex, goToNext, goToPrev)
- Move renderAtom() logic
- Move navigation buttons
- Accept props: lesson, atoms, onAtomComplete, onLessonComplete

This isolates the old flow so we can use it as fallback while building the adaptive flow.

DO NOT remove any functionality from learn page yet - just extract and duplicate for now.
  </action>
  <verify>npm run build succeeds, new component file exists with ~150 lines</verify>
  <done>LinearLessonView.tsx created with extracted navigation logic, compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add session state and mode toggle to learn page</name>
  <files>src/app/learn/page.tsx</files>
  <action>
Add state for adaptive mode:
```typescript
const [learningMode, setLearningMode] = useState<'adaptive' | 'linear'>('adaptive');
const [activeSession, setActiveSession] = useState<LearningSession | null>(null);
const [sessionItemIndex, setSessionItemIndex] = useState(0);
```

Import AdaptiveSessionView and LearningSession type.

Add conditional rendering in main content area:
- If learningMode === 'adaptive' && !activeSession: show AdaptiveSessionView
- If learningMode === 'adaptive' && activeSession: show current session item
- If learningMode === 'linear': use existing linear flow

Add small toggle in header for testing: "Adaptive" / "Linear" mode switch (can be removed later).

Keep all existing imports and functionality - just add new branching logic.
  </action>
  <verify>npm run build succeeds, learn page loads without errors, toggle visible</verify>
  <done>Learn page has adaptive/linear mode state, AdaptiveSessionView renders when adaptive mode selected</done>
</task>

<task type="auto">
  <name>Task 3: Connect session callbacks to learn page</name>
  <files>src/app/learn/page.tsx</files>
  <action>
Wire AdaptiveSessionView callbacks:

1. onStartSession:
   - Set activeSession state
   - Set sessionItemIndex to 0
   - Log "Starting session with X items"

2. onItemComplete:
   - Increment sessionItemIndex
   - Call existing handleAtomComplete with appropriate XP
   - If all items complete, show completion and redirect to dashboard

3. Add availableMinutes calculation:
   - Default to 30 minutes
   - Could read from user preferences later

Pass userId from user.id (already available from useUser hook).

Ensure session items are passed to correct atom components based on item.type.
  </action>
  <verify>npm run build succeeds, console shows "Starting session..." when clicking Start Session button</verify>
  <done>AdaptiveSessionView callbacks connected, session starts and advances through items</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Learn page loads at /learn
- [ ] Toggle between Adaptive/Linear mode works
- [ ] In Adaptive mode, session overview appears
- [ ] Clicking "Start Session" logs to console and advances state
- [ ] Linear mode still works exactly as before
</verification>

<success_criteria>

- LinearLessonView.tsx extracted as reusable component
- Learn page supports both adaptive and linear modes
- AdaptiveSessionView integrated and rendering
- Session callbacks connected and functioning
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-adaptive-flow/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Integrate AdaptiveSessionView

**[Substantive summary of what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Decisions and rationale]

## Issues Encountered

[Problems and resolutions]

## Next Step

Ready for 01-02-PLAN.md (pre-test flow and session item rendering)
</output>
